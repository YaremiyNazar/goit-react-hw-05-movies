import css from "../Notification/Notificatiom.module.css"

const Notification = () => {
  return (
      <p className={css.load__text}>We're sorry, but you've reached the end of search results</p>

  );
};
export default Notification;
